{
  "name": "finsmart-track-render-entrypoint",
  "version": "1.0.0",
  "private": true,
  "description": "Render entrypoint for the finsmart-track backend service.",
  "engines": {
    "node": "20"
  },
  "scripts": {
    "postinstall": "npm --prefix finsmart-track install",
    "build": "npm --prefix finsmart-track install",
    "start": "npm --prefix finsmart-track start",
    "dev": "npm --prefix finsmart-track run dev",
    "lint": "npm --prefix finsmart-track run lint"
  }
}
